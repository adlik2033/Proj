<Window x:Class="Проект.auth.Captcha"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Проект.auth"
        mc:Ignorable="d"
        Title="Captcha" Height="250" Width="250" Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Image x:Name="Captha" Grid.Row="0" Margin="15 15 15 15">
        </Image>
        <TextBox x:Name="textCheck" Grid.Row="1" />
        <CheckBox Content="Я точно не робот" Grid.Row="2" Name="Check"/>
        <Button Content="Проверить" Grid.Row="3" Click="Button_Click"/>
    </Grid>
</Window>
